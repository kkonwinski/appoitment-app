<form method="post" action="{{ path('admin_company_address_delete', {'id': company_address.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ company_address.id) }}">
    <button class="btn btn-outline-danger">{{ 'view.delete'|trans }}</button>
</form>
