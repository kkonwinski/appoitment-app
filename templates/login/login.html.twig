{% extends 'base.html.twig' %}


{% block body %}
    {% if error %}
        <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}

{#    <form action="{{ path('app_login') }}" method="post">#}
{#        <label for="username">Email:</label>#}
{#        <input type="text" id="username" name="_username"/>#}

{#        <label for="password">Password:</label>#}
{#        <input type="password" id="password" name="_password"/>#}

{#        #}{# If you want to control the URL the user is redirected to on success#}
{#        <input type="hidden" name="_target_path" value="/account"/> #}
{#        <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">#}

{#        <button type="submit">login</button>#}
{#    </form>#}

    <div class="container">

        <!-- Outer Row -->
        <div class="row justify-content-center">

            <div class="col-xl-10 col-lg-12 col-md-9">

                <div class="card o-hidden border-0 shadow-lg my-5">
                    <div class="card-body p-0">
                        <!-- Nested Row within Card Body -->
                        <div class="row">
                            <div class="col-lg-6 d-none d-lg-block admin-bg-login-image"></div>
                            <div class="col-lg-6">
                                <div class="p-5">
                                    <div class="text-center">
                                        <h1 class="h4 text-gray-900 mb-4">{{ 'login.login_title'|trans }}</h1>
                                    </div>
                                    <form class="user" action="{{ path('app_login') }}" method="post">
                                        <div class="form-group">
                                            <input type="email" class="form-control form-control-user" name="_username"
                                                   id="username" aria-describedby="emailHelp"
                                                   placeholder={{ 'login.email'|trans }}>
                                        </div>
                                        <div class="form-group">
                                            <input type="password" class="form-control form-control-user" name="_password"
                                                   id="password" placeholder={{ 'login.password'|trans }}>
                                        </div>
{#                                        <div class="form-group">#}
{#                                            <div class="custom-control custom-checkbox small">#}
{#                                                <input type="checkbox" class="custom-control-input" id="customCheck">#}
{#                                                <label class="custom-control-label" for="customCheck">Remember#}
{#                                                    Me</label>#}
{#                                            </div>#}
{#                                        </div>#}
                                        <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
                                        <button class="btn btn-primary btn-user btn-block" type="submit">{{ 'login.login_btn'|trans }}</button>
                                        <hr>
                                        <a href="#" class="btn btn-google btn-user btn-block">
                                            <i class="fab fa-google fa-fw"></i> {{ 'login.login_with_google'|trans }}
                                        </a>
                                        <a href="#" class="btn btn-facebook btn-user btn-block">
                                            <i class="fab fa-facebook-f fa-fw"></i> {{ 'login.login_with_facebook'|trans }}
                                        </a>
                                    </form>
                                    <hr>
{#                                    <div class="text-center">#}
{#                                        <a class="small" href="forgot-password.html">Forgot Password?</a>#}
{#                                    </div>#}
{#                                    <div class="text-center">#}
{#                                        <a class="small" href="register.html">Create an Account!</a>#}
{#                                    </div>#}
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

            </div>

        </div>

    </div>

{% endblock %}
{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('custom/js/login.js') }}"></script>
{% endblock %}